{% extends 'TemplateTrainer.html.twig' %}
{% block title %} Dashborad | Profil {% endblock %}

{% block body %}
<section class="section profile">
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body profile-card pt-4 d-flex flex-column align-items-center">
                        <h2>Nom : </h2>
                        <h2>{{ user.nom }} </h2>

                        <h2>Prenom : </h2>
                        <h2>{{ user.prenom }} </h2>

                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-body pt-3">
                    <!-- Bordered Tabs -->
                    <ul class="nav nav-tabs nav-tabs-bordered">
                        Profil Details
                    </ul>
                    <div class="tab-content pt-2">
                        <div class="tab-pane fade show active profile-overview" id="profile-overview">

                            <table class="table">
                                <tbody>
                                <tr>
                                    <th>Email</th>
                                    <td>{{ user.email }}</td>
                                </tr>
                                <tr>
                                    <th>Mail</th>
                                    <td>{{ user.mail }}</td>
                                </tr>
                                <tr>
                                    <th>DateNaissance</th>
                                    <td>{{ user.DateNaissance ? user.DateNaissance|date('Y-m-d') : '' }}</td>
                                </tr>
                                <tr>
                                    <th>NumeroTel</th>
                                    <td>{{ user.NumeroTel }}</td>
                                </tr>
                                <tr>
                                    <th>Education</th>
                                    <td>{{ user.Education }}</td>
                                </tr>
                                <tr>
                                    <th>Niveau</th>
                                    <td>{{ user.Niveau }}</td>
                                </tr>
                                <tr>
                                    <th>Diplome</th>
                                    <td>{{ user.diplome }}</td>
                                </tr>
                                </tbody>
                            </table>

                    </div><!-- End Bordered Tabs -->
                        <a href="{{ path('app_user_edit', {'id': user.id}) }}">Edit</a>

                </div>
            </div>

        </div>
    </div>
</section>
{% endblock %}
